ltm node /Common/host.example.com {
    # this is a comment
    address 10.2.3.4
    description "A description can contain (almost) any characters!" # and comment can be in-lined.
    fqdn {
        address-family all
        autopopulate enabled
        name host.example.com
    }
    address 10.2.3.123
}
